/// Resolved [cityId] / [routeId] for albergue screens when navigation args omit them.
class AlbergueNavigationIds {
  const AlbergueNavigationIds({this.cityId, this.routeId});

  final int? cityId;
  final int? routeId;
}
