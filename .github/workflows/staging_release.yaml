name: camino_ninja_flutter (staging)

on:
  workflow_dispatch:
    inputs:
      skip_android:
        description: 'Skip Android (AAB build + Play upload)'
        type: boolean
        default: false
      skip_ios:
        description: 'Skip iOS (IPA build + TestFlight upload)'
        type: boolean
        default: false

jobs:
  build_staging:
    uses: ./.github/workflows/_release.yaml
    with:
      flavor: staging
      main_target: lib/main_staging.dart
      release_tag_suffix: staging
      build_appbundle: ${{ inputs.skip_android != true }}
      build_apk: false
      build_ios: ${{ inputs.skip_ios != true }}
      deploy_to_play: ${{ inputs.skip_android != true }}
      deploy_to_testflight: ${{ inputs.skip_ios != true }}
      package_name: ninja.camino.app.stg
    secrets: inherit
